<!DOCTYPE html>
<html>
<head>
	<title>{{title}}</title>
	<link rel="stylesheet" type="text/css" href="/css/bootstrap.css">
	<link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css">
	<link rel="stylesheet" type="text/css" href="/css/style.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
  <link href="http://fontawesome.io/assets/font-awesome/css/font-awesome.css" rel="stylesheet" media="screen"> 
</head>
<body>
	<div class="container">
      <div class="header clearfix">
        <nav>
          <ul class="nav nav-pills pull-right">
            {{#if user}}<!-- if user is logged in show the following --> 
            	<li role="presentation">
                <a href="/">Music Collection</a> 
              </li>
              <li role="presentation">
                <a href="/users/favourites/musicProfile">Favourites</a> <!--goes to Route users method favourites/musicProfile-->
              </li>                      
              <li role="presentation">
                <a href="/users/userProfile">User Profile</a>
              </li>
            	<li role="presentation">
                <a href="/users/logout">Logout</a>
              </li>
            {{else}}  <!-- if user is not logged in show the Login and Register -->
    		 	    <li role="presentation">
                <a href="/users/login">Login</a>
              </li>
            	<li role="presentation">
                <a href="/users/register">Register</a>
              </li>
            {{/if}}
          </ul>
        </nav>
        <h3 class="text-muted">Audio Vibes</h3>
      </div>
      <div class="row">
        <div class="col-lg-12">
        	{{#if success_msg}} <!--check for global variable success_mgs-->
        		<div class="alert alert-success">
        			{{success_msg}}
        		</div>
        	{{/if}}
        	{{#if error_msg}} <!--check for global variable error_msg-->
        		<div class="alert alert-danger">
        			{{error_msg}}
        		</div>
        	{{/if}}
        	{{#if error}} <!--check for global variable error-->
        		<div class="alert alert-danger">
        			{{error}}
        		</div>
        	{{/if}}
        	{{{body}}}
        </div>
      </div>

      <footer class="footer">
        <p>© 2016 Audio Vibes.</p>
      </footer>

    </div>
</body>
</html>